{
    "liveServer.settings.port": 5501,
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "user_data.db",
            "database": "${workspaceFolder:otp}/instance/user_data.db"
        }
    ]
}